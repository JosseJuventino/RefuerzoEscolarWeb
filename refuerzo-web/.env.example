
NEXT_PUBLIC_API_URL=http://localhost:3005

# --- NextAuth v5 (OBLIGATORIA) -----------------------------------------------
# Secreto para cifrar/firmar la cookie de sesión
# se crea con: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=cambia-esto-por-un-secreto-largo-y-aleatorio

NEXT_PUBLIC_SITE_KEY_RECAPTCHA=
